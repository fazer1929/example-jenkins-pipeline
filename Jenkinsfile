pipeline{
  agents any
  stages{
    stage("INIT"){
      steps{
        echo "INIT STAGE RUNNING"
        ls -l
      }
    }
     stage("BUILD"){
      steps{
        echo "BUILD RUNNING RUNNING"
        ls -a
      }
    }
  }
}
